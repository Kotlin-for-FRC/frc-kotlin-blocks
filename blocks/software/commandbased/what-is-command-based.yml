block0: |2-
    Trigger(condition::get).onTrue(Commands.runOnce({ piston.set(DoubleSolenoid.Value.kForward) }))
block1: |2-
    if (condition.get()) {
      if (!pressed) {
        piston.set(DoubleSolenoid.Value.kForward)
        pressed = true
      }
    } else {
      pressed = false
    }
