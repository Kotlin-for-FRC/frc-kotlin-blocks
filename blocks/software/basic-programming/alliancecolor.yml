block0: |
  val ally: Optional<Alliance> = DriverStation.getAlliance()
  when (ally.getOrNull()){
       null -> <NO COLOR YET ACTION>
       DriverStation.Alliance.RED -> <RED ACTION>
       DriverStation.Alliance.Blue -> <BLUE ACTION>
 }
