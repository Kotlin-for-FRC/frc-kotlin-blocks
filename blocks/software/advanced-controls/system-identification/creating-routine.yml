block0: |2-
    // Creates a SysIdRoutine
    routine: SysIdRoutine = SysIdRoutine(
        SysIdRoutine.Config(),
        SysIdRoutine.Mechanism(this::voltageDrive, this::logMotors, this)
    );
block1: |2-
    fun sysIdQuasistatic(direction: SysIdRoutine.Direction) = routine.quasistatic(direction);
    
    fun sysIdDynamic(direction: SysIdRoutine.Direction) = routine.dynamic(direction);