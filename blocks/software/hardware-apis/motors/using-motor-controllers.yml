# validated: 2024-10-06 TW e53db15a2003 software/hardware-apis/motors/using-motor-controllers.rst
block0: |
  val spark = new Spark(0) // 0 is the RIO PWM port this is connected to
  spark.set(-0.75) // the % output of the motor, between -1 and 1
  val victor = VictorSP(0) // 0 is the RIO PWM port this is connected to
  victor.set(0.6) // the % output of the motor, between -1 and 1
